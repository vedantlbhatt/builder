{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "GENERATED by scripts/gen_narrative.py from spec/narrative.v1.json — DO NOT EDIT. Handed to `claude -p --json-schema`.",
  "title": "BuilderNarrative",
  "x-version": 1,
  "description": "How one person works, written back to them from measurements of their own sessions. Every claim carries a number that appears in the input. A number that does not appear in the input is the worst thing this document can contain, because the reader will believe it.",
  "type": "object",
  "properties": {
    "archetype_line": {
      "description": "what this person's archetype means for THEM, naming the measurement that earned it. Never a definition of the archetype in general. Empty when the check took it back. (max 220 chars)",
      "type": "string",
      "maxLength": 220
    },
    "how_you_work": {
      "description": "the shape of their working day: how they open a session, how much rope they give the agent, when they step in, how they verify. Every paragraph cites at least one number from the input. (each max 400 chars, 2 to 4 items)",
      "type": "array",
      "items": {
        "type": "string",
        "maxLength": 400
      },
      "maxItems": 4,
      "minItems": 2
    },
    "strengths": {
      "description": "what they are good at, each with the measurement under it. (1 to 3 items)",
      "type": "array",
      "items": {
        "$ref": "#/$defs/NarrativeClaim"
      },
      "maxItems": 3,
      "minItems": 1
    },
    "watch_outs": {
      "description": "what the same numbers suggest is costing them, each with the measurement under it. Never a scolding. (1 to 3 items)",
      "type": "array",
      "items": {
        "$ref": "#/$defs/NarrativeClaim"
      },
      "maxItems": 3,
      "minItems": 1
    },
    "one_experiment": {
      "description": "one concrete thing to try next session, small enough to do, tied to a number above so they can tell whether it worked. (max 260 chars)",
      "type": "string",
      "maxLength": 260
    }
  },
  "required": [
    "archetype_line",
    "how_you_work",
    "strengths",
    "watch_outs",
    "one_experiment"
  ],
  "additionalProperties": false,
  "$defs": {
    "NarrativeClaim": {
      "type": "object",
      "properties": {
        "text": {
          "description": "the claim, in the second person, no numbers needed here. (max 160 chars)",
          "type": "string",
          "maxLength": 160
        },
        "evidence": {
          "description": "the numbers behind it, copied from the input. Never a number the input did not contain. (max 160 chars)",
          "type": "string",
          "maxLength": 160
        }
      },
      "required": [
        "text",
        "evidence"
      ],
      "additionalProperties": false
    }
  }
}
