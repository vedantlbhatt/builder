// GENERATED by scripts/gen_narrative.py from spec/narrative.v1.json — DO NOT EDIT.

export const NARRATIVE_VERSION = 1;

/** Character caps by size class; string fields below name one of these in their doc. */
export const NARRATIVE_MAX_LENGTHS = {
  line: 220,
  para: 400,
  claim: 160,
  experiment: 260,
  model: 80,
} as const;

export interface NarrativeClaim {
  /** the claim, in the second person, no numbers needed here. (max 160 chars) */
  text: string;
  /** the numbers behind it, copied from the input. Never a number the input did not contain. (max 160 chars) */
  evidence: string;
}

export interface BuilderNarrative {
  /** what this person's archetype means for THEM, naming the measurement that earned it. Never a definition of the archetype in general. Empty when the check took it back. (max 220 chars) */
  archetype_line: string;
  /** the shape of their working day: how they open a session, how much rope they give the agent, when they step in, how they verify. Every paragraph cites at least one number from the input. (each max 400 chars, 2 to 4 items) */
  how_you_work: string[];
  /** what they are good at, each with the measurement under it. (1 to 3 items) */
  strengths: NarrativeClaim[];
  /** what the same numbers suggest is costing them, each with the measurement under it. Never a scolding. (1 to 3 items) */
  watch_outs: NarrativeClaim[];
  /** one concrete thing to try next session, small enough to do, tied to a number above so they can tell whether it worked. (max 260 chars) */
  one_experiment: string;
  /** == spec version. A retuned prompt is a recompute, not a migration. */
  narrative_version: number;
  /** model id that wrote this, as reported by claude -p (max 80 chars) */
  model: string;
  /** (ISO 8601) */
  generated_at: string;
  /** dashes rewritten as commas after the fact. Rewritten, never rejected: throwing a paid call away over punctuation is not a trade. */
  dashes_rewritten: number;
  /** claims deleted because they carried a number the input did not contain. Above zero means the ban is doing work; the claims themselves are logged. */
  invented_numbers_dropped: number;
}
