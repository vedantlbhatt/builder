// GENERATED by scripts/gen_shipped.py from spec/shipped.v1.json — DO NOT EDIT.

export const SHIPPED_VERSION = 1;

/** Character caps by size class; string fields below name one of these in their doc. */
export const SHIPPED_MAX_LENGTHS = {
  line: 100,
  sentence: 200,
  para: 400,
  short: 60,
} as const;

export interface ShippedChange {
  /** one thing that is now possible that was not before, in plain words. Not a commit message, not a file name. (max 200 chars) */
  text: string;
  /** what in the work shows it: a commit, a test, a number the input gave you. Null when the input only described it. (max 200 chars) */
  evidence?: string | null;
}

export interface ShippedPost {
  /** one line: what this is. Written for somebody who has never seen the project. Not the repo name, not a commit message, no jargon a non-programmer would miss. (max 100 chars) */
  what: string;
  /** who it is for and what it lets them do, in one sentence. Null when the work genuinely does not say, which is better than a guess. (max 200 chars) */
  why?: string | null;
  /** how far along it is, from what the work shows. Tests and commits say working; a first scaffold says prototype. */
  stage: Stage;
  /** what is new since last time, each a thing somebody could try. Never a list of files touched. (1 to 4 items) */
  changes: ShippedChange[];
  /** the thing that was genuinely difficult, and what it turned out to be. THE FIELD THIS SPEC EXISTS FOR. Grounded in the evidence you were given, never invented. Null when nothing in the work was hard, which is a real answer. (max 400 chars) */
  hard_part?: string | null;
  /** technologies actually visible in the work. Never inferred from the kind of project it looks like. (each max 60 chars, max 6 items) */
  stack: string[];
  /** the one screenshot or clip that would show this off, named concretely. Null when there is nothing visual to show. (max 200 chars) */
  demo?: string | null;
  /** what is obviously next, only when the work itself says so (a TODO, a failing test left in, a stated intent). Null otherwise. (max 200 chars) */
  next?: string | null;
  /** == spec version. A retuned prompt is a recompute, not a migration. */
  shipped_version: number;
  /** model id that wrote this, as reported by claude -p (max 60 chars) */
  model: string;
  /** (ISO 8601) */
  generated_at: string;
  /** dashes rewritten as commas after the fact. Rewritten, never rejected. */
  dashes_rewritten: number;
  /** claims deleted for naming a technology or a fact the work never showed. Above zero means the check is doing work. */
  unsupported_claims_dropped: number;
}
